# Git files
.git
.gitignore

# Node modules (will be installed in Docker)
node_modules

# Next.js build outputs (will be generated in Docker)
.next

# Development environment files
.env
.env*.local
.env.development*
.env.test*
.env.production*

# Editor and OS specific files
.vscode
.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Firebase specific files that might be local but not needed in image
# If you have a service account JSON for admin SDK that is mounted or env var based, exclude it
# e.g. firebase-service-account.json

# Test files
coverage
src/**/*.test.ts
src/**/*.test.tsx
src/**/*.spec.ts
src/**/*.spec.tsx

# Storybook specific (if used)
.storybook
storybook-static

# Husky specific (if used)
.husky

# Patch-package specific (if used and patches are applied during local dev only)
patches/
.patch-pid

# Files from Firebase App Hosting (not needed for Cloud Run Docker deployment)
apphosting.yaml
